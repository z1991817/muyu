[package]
name = "error"
version = "2.2.2"
edition = "2024"

[dependencies]
error-derive = { path = "../error-derive" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "error"
path = "src/lib.rs"
