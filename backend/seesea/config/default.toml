# SeeSea 默认配置文件
# 这是 SeeSea 元搜索引擎的默认配置，包含所有必要的设置

# =============================================================================
# 通用配置
# =============================================================================
[general]
# 应用实例名称
instance_name = "SeeSea"
# 是否启用调试模式
debug = false
# 引擎加载模式: "global" 或 "settings"
engine_loading_mode = "global"
# 是否启用指标收集
enable_metrics = true
# 默认语言
default_lang = "auto"
# 时区
timezone = "UTC"
# 应用版本
version = "2.0.4"
# 运行环境: "development", "testing", "staging", "production"
environment = "production"

# =============================================================================
# 服务器配置
# =============================================================================
[server]
# 绑定地址
bind_address = "0.0.0.0"
# 端口号
port = 8080
# 是否为公共实例
public_instance = false
# 服务密钥（生产环境请更改！）
secret_key = "change-me-in-production-please-generate-a-strong-secret-key"
# 基础 URL
# base_url = "https://your-seesea-domain.com"

# 前端 API 地址配置
# 留空表示使用同源（默认，后端驱动前端时使用）
# 设置后会覆盖同源配置，适用于 nginx 反向代理等场景
# 例如: frontend_api_url = "https://api.example.com"
frontend_api_url = ""

# TLS 配置（HTTPS）
[server.tls]
enabled = false
# cert_path = "/path/to/cert.pem"
# key_path = "/path/to/key.pem"
# ca_path = "/path/to/ca.pem"
# verify_client = false

# =============================================================================
# 搜索配置
# =============================================================================
[search]
# 安全搜索级别: 0=无, 1=中等, 2=严格
safe_search = "none"
# 自动完成引擎
autocomplete = ""
# 支持的输出格式
formats = ["json", "html", "csv", "rss"]
# 默认每页结果数
results_per_page = 10
# 最大结果数
max_results_per_page = 50
# 搜索超时时间（秒）
search_timeout = 30
# 最大并发引擎数
max_concurrent_engines = 5
# 默认语言
default_language = "auto"
# 支持的语言
supported_languages = ["en", "zh", "ja", "ko", "es", "fr", "de", "ru"]
# 是否支持时间范围
time_range_support = true

# 结果聚合配置
[search.aggregation]
# 启用结果去重
enable_deduplication = true
# 去重算法
deduplication_method = "url_and_title"
# 启用结果排序
enable_ranking = true
# 排序算法
ranking_algorithm = "hybrid"
# 最大聚合结果数
max_results = 100
# 启用结果分组
enable_grouping = true
# 分组策略
grouping_strategy = "smart"

# 查询处理配置
[search.query_processing]
# 启用查询扩展
enable_expansion = true
# 启用查询纠正
enable_correction = true
# 纠正阈值
correction_threshold = 0.8
# 启用同义词扩展
enable_synonyms = true
# 启用停用词过滤
enable_stop_words = true
# 最大查询长度
max_query_length = 200
# 最小查询长度
min_query_length = 1

# =============================================================================
# 隐私保护配置
# =============================================================================
[privacy]

# User-Agent 轮换配置
[privacy.user_agent_rotation]
enabled = true
# 轮换策略
rotation_strategy = "random"
# 轮换间隔（请求数）
rotation_interval = 10
# 是否包含移动端 UA
include_mobile = false
# 按浏览器类型分组
group_by_browser = true

# TLS 指纹保护配置
[privacy.fingerprint_protection]
# 保护级别: "none", "basic", "advanced", "maximum"
protection_level = "basic"
# 随机化 TLS 扩展
randomize_extensions = true
# 随机化密码套件
randomize_cipher_suites = true
# 模拟常见浏览器
emulate_browsers = true

# 请求时序随机化配置
[privacy.request_timing]
# 时序策略
timing_strategy = "light"
# 最小延迟（毫秒）
min_delay = 100
# 最大延迟（毫秒）
max_delay = 2000
# 基于请求大小调整延迟
size_based_delay = true
# 基于引擎调整延迟
engine_based_delay = true

# DNS 配置
[privacy.dns_config]
# 是否启用 DNS over HTTPS
enabled = true
# DNS 服务器列表（包含国内外服务商）
[[privacy.dns_config.servers]]
name = "Cloudflare"
url = "https://cloudflare-dns.com/dns-query"
enabled = true
weight = 1.0

[[privacy.dns_config.servers]]
name = "Google"
url = "https://dns.google/dns-query"
enabled = true
weight = 1.0

[[privacy.dns_config.servers]]
name = "阿里云"
url = "https://dns.alidns.com/dns-query"
enabled = true
weight = 1.2

[[privacy.dns_config.servers]]
name = "腾讯 DNSPod"
url = "https://doh.pub/dns-query"
enabled = true
weight = 1.2

[[privacy.dns_config.servers]]
name = "360 DoH"
url = "https://doh.360.cn/dns-query"
enabled = true
weight = 1.1

# DNS 超时时间（毫秒）
timeout = 5000
# 重试次数
retry_count = 2
# 是否启用 DNS 缓存
enable_cache = true
# 缓存过期时间（秒）
cache_ttl = 300

# 请求头配置
[privacy.headers]
# 移除隐私敏感头
remove_privacy_headers = true
# 标准化 Accept 头
normalize_accept = true
# 随机化其他头
randomize_headers = false

# Cookie 处理配置
[privacy.cookie_handling]
# 是否接受 Cookie
accept_cookies = false
# 是否发送 Cookie
send_cookies = false
# 过滤策略
filter_policy = "disabled"

# =============================================================================
# 缓存配置
# =============================================================================
[cache]
# 缓存后端: "rocks_db", "redis", "memory", "hybrid", "custom"
# RocksDB: 嵌入式数据库（默认，持久化到磁盘）
# Redis: 分布式缓存
# Memory: 纯内存缓存（最快）
# Hybrid: 混合缓存（内存 + 磁盘）
# Custom: 自定义后端
backend = "memory"
# 数据库路径（使用 .seesea 目录）
database_path = ".seesea/cache.db"
# 缓存过期时间（秒）
ttl = 3600
# 最大缓存大小（字节）
max_size = 1073741824  # 1GB
# 是否启用结果缓存
enable_result_cache = true
# 是否启用元数据缓存
enable_metadata_cache = true
# 是否启用 DNS 缓存
enable_dns_cache = true
# 是否启用 RSS 缓存
enable_rss_cache = true
# 缓存刷新间隔（秒）
refresh_interval = 300
# 淘汰策略
eviction_policy = "ttl"

# 压缩配置
[cache.compression]
# 是否启用压缩
enabled = true
# 压缩算法
algorithm = "lz4"
# 压缩阈值（字节）
threshold = 1024
# 压缩级别
level = 3

# 监控配置
[cache.monitoring]
# 是否启用监控
enabled = true
# 指标收集间隔（秒）
metrics_interval = 60
# 是否启用慢查询日志
enable_slow_query_log = true
# 慢查询阈值（毫秒）
slow_query_threshold = 1000

# =============================================================================
# API 配置
# =============================================================================
[api]
# API 版本
version = "v1"
# 是否启用 CORS
enable_cors = true

# CORS 配置
[api.cors]
# 允许的源
allowed_origins = ["*"]
# 允许的方法
allowed_methods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
# 是否允许凭证
allow_credentials = false

# 速率限制配置
[api.rate_limit]
# 是否启用速率限制
enabled = true
# 每秒请求数
requests_per_second = 10
# 每分钟请求数
requests_per_minute = 100
# 每小时请求数
requests_per_hour = 1000
# 每天请求数
requests_per_day = 10000
# 突发请求限制
burst_size = 20

# 认证配置
[api.auth]
# 是否启用认证
enabled = true
# 认证类型: "none", "api_key", "jwt", "basic"
auth_type = "api_key"

# API 密钥配置
[api.auth.api_key]
enabled = true
header_name = "Authorization"
query_param = "magic_token"
key_prefix = "Bearer"

[[api.auth.api_key.api_keys]]
name = "default"
key_hash = "change-me-in-production"
permissions = ["*"]
enabled = true
created_at = "2025-01-01T00:00:00Z"

# JWT 配置
[api.auth.jwt]
enabled = false
secret = "jwt_secret_key_change_in_production"
algorithm = "HS256"
expiry = 3600
refresh_expiry = 86400
issuer = "seesea"
audience = "api"

# 响应格式配置
[api.response_format]
# 默认格式
default_format = "json"
# 支持的格式
supported_formats = ["json", "xml", "csv"]
# 是否包含调试信息
include_debug_info = false
# 是否包含性能指标
include_metrics = true
# 是否包含请求 ID
include_request_id = true

# 响应压缩配置
[api.response_format.compression]
# 是否启用
enabled = true
# 压缩算法
algorithms = ["gzip", "deflate"]
# 压缩阈值
threshold = 1024

# API 安全配置
[api.security]
# 是否强制 HTTPS
force_https = false

# API 文档配置
[api.documentation]
# 是否启用
enabled = false
# 文档类型
doc_type = "openapi3"
# 文档路径
path = "/docs"
# 是否包含示例
include_examples = true

# =============================================================================
# 引擎配置
# =============================================================================
[engines]

# 全局引擎设置
[engines.global_settings]
# 默认超时时间（秒）
default_timeout = 30
# 默认重试次数
default_retries = 3
# 最大并发引擎数
max_concurrent_engines = 5
# 引擎失败阈值（0.0-1.0）
failure_threshold = 0.5
# 引擎恢复时间（秒）
recovery_time = 300
# 是否启用引擎监控
enable_monitoring = true
# 是否启用性能统计
performance_stats = true

# 引擎发现配置
[engines.discovery]
# 是否启用自动发现
enabled = true
# 发现路径
discovery_paths = ["./engines"]
# 是否启用热重载
enable_hot_reload = false

# 健康检查配置
[engines.health_check]
# 是否启用健康检查
enabled = true
# 检查间隔（秒）
check_interval = 300
# 检查超时（秒）
check_timeout = 10
# 失败阈值
failure_threshold = 3
# 恢复阈值
recovery_threshold = 2

# =============================================================================
# 日志配置
# =============================================================================
[logging]
# 日志级别: "error", "warn", "info", "debug", "trace"
level = "info"
# 日志格式: "simple", "full", "json", "compact"
format = "full"
# 日志输出: "stdout", "stderr", "file", "both"
output = "stdout"
# 是否启用结构化日志
structured = true
# 是否启用彩色输出
colored = true

# 模块级别日志配置
[logging.module_levels]
# 是否启用模块级别配置
enabled = false
# 默认模块级别
default_level = "info"
# 忽略的模块
ignore_modules = ["hyper", "tokio", "reqwest"]

# 日志性能配置
[logging.performance]
# 是否启用性能日志
enabled = true
# 异步写入
async_write = true
# 缓冲区大小
buffer_size = 8192
# 刷新间隔（毫秒）
flush_interval = 1000
# 是否启用批量写入
batch_write = true
# 批量大小
batch_size = 100

# 日志监控配置
[logging.telemetry]
# 是否启用遥测
enabled = false
# 导出间隔（秒）
export_interval = 60
# 采样率
sampling_rate = 1.0

# =============================================================================
# RSS Feed 配置
# =============================================================================
[rss]
# 是否启用 RSS 功能
enabled = true
# RSS 模板目录
template_dir = "rss/template"
# 配置文件路径
config_path = ".seesea/rss_config.toml"
# 默认更新间隔（秒）
default_update_interval = 3600  # 1 hour
# 最大保留项目数
max_items_per_feed = 1000
# 是否启用自动更新
auto_update = true
# 启动时更新持久化 RSS
update_on_startup = true

# 持久化 RSS Feeds（从模板添加的 feeds 会自动添加到这里）
[[rss.persistent_feeds]]
# 示例：取消注释以启用
# name = "Xinhua News - Politics"
# url = "https://www.xinhuanet.com/politics/news_politics.xml"
# update_interval = 3600
# enabled = true

# =============================================================================
# 语义缓存配置
# =============================================================================
[cache.semantic]
# 是否启用语义缓存
enabled = true
# 相似度阈值（0.0-1.0，建议0.7-0.85）
similarity_threshold = 0.75
# 每个查询最大返回结果数
max_results_per_query = 50
# 是否启用跨查询去重
enable_deduplication = true

# =============================================================================
# 向量数据库配置
# =============================================================================
[vector_store]
# 是否启用向量数据库
enabled = true
# 向量数据库类型
provider = "Qdrant"
# 向量维度
dimension = 1536
# 集合名称
collection_name = "seesea_pro"
# 距离度量
distance = "Cosine"
# 日志级别
log_level = "info"

# Qdrant 特定配置
[vector_store.qdrant]
# Qdrant 服务器 URL
url = "http://localhost:6334"
# API 密钥
api_key = ""
# 使用 TLS
use_tls = false
# GRPC 端口
grpc_port = 6334
# REST 端口
rest_port = 6333
# 超时时间（秒）
timeout = 30
# 最大重试次数
max_retries = 3

# 向量数据库缓存配置
[vector_store.cache]
# 是否启用缓存
enabled = true
# 缓存 TTL（秒）
ttl = 3600
# 最大缓存大小
max_size = 1073741824  # 1GB
# 缓存作用域
scope = "vector"
# 缓存路径
cache_path = ".seesea/vector_cache"

# 动态调整配置
[vector_store.dynamic_adjustment]
# 是否启用动态调整
enabled = false
# 最小批次大小
min_batch_size = 10
# 最大批次大小
max_batch_size = 100
# 最小删除批次大小
min_delete_batch_size = 10
# 最大删除批次大小
max_delete_batch_size = 50
# 批次大小调整阈值
batch_size_adjustment_threshold = 50
# HNSW 参数 m 调整范围
hnsw_m_range = [10, 48]
# HNSW 参数 ef_construct 调整范围
hnsw_ef_construct_range = [100, 500]
# 是否调整 HNSW 参数
adjust_hnsw_params = false

# 向量数据库统计信息配置
[vector_store.stats]
# 是否启用统计信息收集
enabled = false
# 统计信息收集间隔（秒）
collection_interval = 3600
# 统计信息保留时间（天）
retention_days = 30
# 是否启用实时统计
realtime = false
# 是否启用趋势分析
enable_trend_analysis = false